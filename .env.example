# PostHog 项目 API key（phc_ 开头）
# 不设置或留空 → 埋点会被静默丢弃（dev 模式控制台会有一次 warn）
# 获取：https://posthog.com → Settings → Project → Project API key
# 区域：当前代码 hardcode 用 us.i.posthog.com，注册时也选 US 即可
#
# 用法：
#   cp .env.example .env.local
#   把下面这行的 phc_xxxx 换成真的 key
#   重启 dev server 或重新 vite build
VITE_POSTHOG_KEY=phc_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
